/**
 * External dependencies
 */
import ReactDOM from 'react-dom';
import React from 'react';

/**
 * Internal dependencies
 */
import App from './app';

ReactDOM.render( <App />, document.getElementById( 'wordpress-importer-root' ) );
